# agent-loop

Reusable GitHub Actions workflows (`workflow_call`) for an issue-driven agent loop.

Public because reusable workflows can only be called across repositories from a public host
repo. Configuration is supplied entirely by the calling repository.

MIT licensed.
